node ('master'){
  env.WORKSPACE = pwd()
  echo "${env.WORKSPACE}"

  stage 'Build'
  checkout scm
  sh 'uptime'

  stage 'Live'
  sh 'echo "promote Live"'

  withDockerContainer(args: "-u root --volume ${env.WORKSPACE}:/code:rw,Z", image: 'centos:7') {
    sh 'rpm -qa | grep -i sudo'
    sh 'ls -la /code'
    sh '/usr/bin/sudo yum install -y ruby'
    sh '/usr/bin/sudo gem install serverspec'
    sh 'cd /code; rake spec'
  }

  stage 'Unstable'
  sh 'echo "promote Unstable"'

  stage 'Testing'
  sh 'echo "promote Testing"'

  timeout(time: 300, unit: 'SECONDS') {
    input message: 'Waiting for approve', ok: 'Approve'
    stage 'Stable'
    sh 'echo "promote Stable"'
  }

}
